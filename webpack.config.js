var path = require('path');
var Webpack = require('webpack');

module.exports = {

    module: {
    
    },
        plugins:[
  ],
    stats: {
        // Nice colored output
        colors: true
    },
    // Create Sourcemaps for the bundle
    devtool: 'source-map',
};
